// Landing hero background image, once ChatGPT-generated artwork exists — see
// LANDING_DESIGN_PROMPTS.md. Null for now: the hero renders its current
// CSS-only look until this is set.
//
// Path convention: after adding public/hero-background.jpg, set this to
// '/hero-background.jpg'. Relative to /public is fine here (unlike the slide
// deck's category backgrounds) — the landing page always renders from the
// live site, never as a standalone offline file.
export const HERO_BACKGROUND_IMAGE: string | null = null;
